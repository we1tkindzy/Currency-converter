export interface DateFormattedData {
  year: number;
  month: string | number;
  day: string | number;
  hour: string | number;
  minute: string | number;
  second: string | number;
}

export interface Coin {
  id: string;
  symbol: string;
  name: string;
}
